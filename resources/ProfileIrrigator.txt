package {{.BasePackage}}.page.irrigator;

import {{.BasePackage}}.security.AuthenticationInfos;
import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Service;
import org.springframework.ui.Model;

@Service
@RequiredArgsConstructor
public class ProfileIrrigator {

    public void irrigateModel(Model model, AuthenticationInfos userInfos, String fragmentAdress) {
        model.addAttribute("user", userInfos);
    }
}

